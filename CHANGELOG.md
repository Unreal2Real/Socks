# 更新日志

## v2.2.0 - 2026-04-30

### 本地数据缓存机制

1. **DataCache 类**
   - 支持日线数据本地缓存
   - 使用 CSV 格式存储，避免额外依赖
   - 元数据存储在 metadata.json
   - 缓存有效期：1天（可配置）

2. **缓存策略**
   - 首次获取：从 Baostock API 读取并缓存
   - 后续获取：直接读取本地文件
   - 缓存路径：`data/cache/` 目录

3. **性能提升**
   - 首次获取：~0.5秒/只（API调用）
   - 缓存命中：~0.01秒/只（本地读取）
   - **速度提升：25-50倍**

4. **新增方法**
   - `get_cache_stats()` - 获取缓存统计信息
   - `clear_cache()` - 清空缓存

5. **新增文件**
   - `data/providers/cache.py` - 缓存实现

6. **修改文件**
   - `data/providers/baostock_provider.py` - 集成缓存
   - `data/providers/hybrid_provider.py` - 支持缓存回退

## v2.1.0 - 2026-04-30

### 股票过滤优化

1. **只保留沪深A股主板**
   - 过滤掉创业板 (300xxx, 301xxx)
   - 过滤掉科创板 (688xxx)
   - 过滤掉北交所 (8xxxxx, 4xxxxx)
   - 过滤掉各类指数（上证、深证、行业指数等）

2. **股票数量优化**
   - 优化前：5329 只（包含指数、创业板、科创板等）
   - 优化后：3110 只（仅沪深A股主板）

3. **指数过滤关键词**
   - 市场指数：上证、深证、综指、成指等
   - 行业指数：消费、高端装备、医药主题等
   - 其他：基金、国债、企债等

## v2.0.0 - 2026-04-30

### 新增功能

1. **混合数据源模式 (Hybrid Provider)**
   - 优先使用通达信本地文件获取日线数据，速度快且离线可用
   - 支持通过 MOOTDX 获取分钟线和实时报价
   - 本地数据缺失时自动切换到 Baostock API

2. **MOOTDX 集成**
   - 支持实时行情获取
   - 支持分钟线数据查询
   - 支持通达信本地文件解析

3. **数据源切换**
   - 命令行参数 `--source` 支持三种模式：
     - `hybrid`: 混合模式（默认）
     - `baostock`: Baostock API
     - `tdx`: 本地通达信数据

### 架构改进

1. **数据抽象层**
   - 创建 `data/providers/` 目录
   - 定义 `DataProvider` 抽象接口
   - 实现多种数据源实现类

2. **工厂模式**
   - 统一数据源创建入口
   - 支持故障转移机制

## v1.0.0 - 初始版本

- 基于 Baostock API 的厂字形态股票分析工具
- 支持股票扫描、回测、统计等功能